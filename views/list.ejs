<!DOCTYPE html>

<html>
  <head></head>
  <body>
    <div>
      <% for (var i = 0; i < urls.length; i++) { %>
      <li><%= urls[i].shortURL %> :<%= urls[i].longURL %></li>
      <% } %>
    </div>
  </body>
</html>
